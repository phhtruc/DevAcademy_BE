server:
  port: 8002
spring:
  config:
    activate:
      on-profile: product
  datasource:
    url: jdbc:mysql://localhost:3306/techlearn
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    locations: classpath:/db/migration
    baseline-on-migrate: true
    baseline-version: 0